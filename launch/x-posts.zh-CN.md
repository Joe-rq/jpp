# J++ X posts — zh-CN

Drafts / 待发布。Copy only the text inside each block / 只复制代码块中的正文。

## zh-CN-T01 · 首发线程

Weighted upper bound / 加权长度上界：131/280

```text
1/10 我们开始做一门语言，叫 J++。

起点是一个直觉：如果问题和求解方法都能保存、传递、组合，组合之后又能继续组合，人们能构造出什么？
```

## zh-CN-T02 · 首发线程

Weighted upper bound / 加权长度上界：148/280

```text
2/10 我们被算法组织信息的力量吸引：可靠的二元回答，配合合适的提问方式，十次就能区分 1,000 个候选。

我们想探索，语义判断与这些算法结合，会产生什么。
```

## zh-CN-T03 · 首发线程

Weighted upper bound / 加权长度上界：139/280

```text
3/10 JEV 让我们想到：判断可以成为一个可调用的操作。围绕它，程序还能选择材料、构造下一道问题、运行精确检查。

这些组织方式，也应该能够复用。
```

## zh-CN-T04 · 首发线程

Weighted upper bound / 加权长度上界：147/280

```text
4/10 我们最看重一条要求：一组组件组合之后，对外仍然是一个组件。

一种方法可以接收另一种方法，也可以返回新的方法。内部越来越复杂，使用方式仍然清楚。
```

## zh-CN-T05 · 首发线程

Weighted upper bound / 加权长度上界：135/280

```text
5/10 这也需要表达不确定性：哪里还没判断清楚，哪些计算已经可以继续？

我们希望保留那一小块未决，让精确计算、搜索与新的证据共同推进程序。
```

## zh-CN-T06 · 首发线程

Weighted upper bound / 加权长度上界：145/280

```text
6/10 现在交付的是 Python 嵌入式语言、运行内核和组合库。

先用可运行的程序发现真正需要的规则，再逐步发展独立语法。我们还不知道它最终会长成什么样。
```

## zh-CN-T07 · 首发线程

Weighted upper bound / 加权长度上界：140/280

```text
7/10 首个离线演示：从 1,000 个候选中用十次提问找到目标。

回答使用合成数据，无须 API Key。它展示提问策略怎样驱动程序，也让每个人都能跑起来。
```

## zh-CN-T08 · 首发线程

Weighted upper bound / 加权长度上界：141/280

```text
8/10 另一个演示不断生成表达式、运行检查、吸收反例。三轮后得到绝对值表达式，并执行全部九个指定输入。

生成器是有限枚举；检查与反馈真的在运行。
```

## zh-CN-T09 · 首发线程

Weighted upper bound / 加权长度上界：139/280

```text
9/10 我们希望它以后用于代码构造、调查与约束规划，也希望别人带来我们没想到的应用。

现在最想看到的是：你用已有基本单元，构造出一种新的方法。
```

## zh-CN-T10 · 首发线程

Weighted upper bound / 加权长度上界：148/280

```text
10/10 J++ 已开源，MIT 许可证。仓库里有安装说明、离线演示、设计动机和路线图。

欢迎运行它，组合它，把难以表达的地方告诉我们。
https://github.com/Towow-ai/jpp
```

## zh-CN-D01 · 十次提问

Weighted upper bound / 加权长度上界：149/280

```text
J++ 的离线例子把 1,000 个候选缩小到一个，用了十次合成的是非回答。

二分算法很经典。这里想让人动手改变的是：选哪一问的策略，如何作为组件被替换和复用。
```

## zh-CN-D02 · 反例反馈

Weighted upper bound / 加权长度上界：134/280

```text
一个小循环：生成候选 → 运行检查 → 把失败输入送回生成器。

J++ 的表达式例子三轮后找到结果。我们想让整段循环也成为一个可以传递的方法。
```

## zh-CN-D03 · 从 Python 开始

Weighted upper bound / 加权长度上界：140/280

```text
J++ 当前用 Python 实现。这样我们能先运行真实的组合，看到接口哪里难用。

独立语法的设计，应该有具体程序告诉我们：它究竟需要让什么更容易表达。
```

## zh-CN-D04 · 问题作为值

Weighted upper bound / 加权长度上界：128/280

```text
我们希望问题像数据一样可以保存、传递和构造。

程序不仅接收答案，还能决定下一道问题是什么。J++ 现在已经有问题的序列化和恢复接口。
```

## zh-CN-D05 · 局部不确定

Weighted upper bound / 加权长度上界：154/280

```text
假设三项观察是：是、是、未知。问“至少两项成立”，已有答案就够了。问“至少三项成立”，仍然未决。

J++ 的计数上下界保留这个区别；结论以已接受的观察为前提。
```

## zh-CN-D06 · 当前任务

Weighted upper bound / 加权长度上界：140/280

```text
J++ 的下一步：把问题、材料和结果接口讲得更清楚，让新读者少猜一步；再让更多算法接进同一套组件。

我们想从这些具体使用里，找到语言还缺的东西。
```

## zh-CN-D07 · 组合的组合

Weighted upper bound / 加权长度上界：136/280

```text
我最在意的是：一组组合做好以后，它还能不能继续组合？

J++ 用同一种组件接口表示小操作和复合方法。我们希望复杂度增长时，复用方法仍然简单。
```

## zh-CN-D08 · 第三种方法

Weighted upper bound / 加权长度上界：144/280

```text
J++ 仓库里还有一个独立编写的预算分配方法。它把判断结果接到精确分配算法上，再封装进更大的组件。

我们正在寻找更多这样的例子，检验接口能复用多远。
```

## zh-CN-D09 · 怎样判断价值

Weighted upper bound / 加权长度上界：133/280

```text
我希望这样判断 J++ 有没有价值：

别人能复用我的方法吗？替换策略要改多少代码？写新算法需要修改内核吗？

欢迎用具体程序来回答这些问题。
```

## zh-CN-D10 · 首版验证

Weighted upper bound / 加权长度上界：155/280

```text
J++ 首版的 25 项机制测试通过，Linux 上 Python 3.12、3.13 两组 CI 也通过。

现在可以下载并离线运行。真实模型质量会用另外的实验评估。
https://github.com/Towow-ai/jpp
```

## zh-CN-D11 · 后端演进

Weighted upper bound / 加权长度上界：135/280

```text
JEV 是 J++ 的起点。我们希望方法的组合结构可以随着判断后端一起发展。

换一个模型后，哪些规则仍成立、哪些需要重测，是接下来要明确的问题。
```

## zh-CN-D12 · 贡献邀请

Weighted upper bound / 加权长度上界：170/280

```text
想参与 J++，可以先写一个现有示例没有的方法，然后把它接入另一段方法。

把难用的地方、重复代码和可运行的例子一起带来。我们会从这些地方继续设计语言。
https://github.com/Towow-ai/jpp
```
