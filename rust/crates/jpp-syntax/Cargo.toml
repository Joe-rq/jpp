[package]
name = "jpp-syntax"
version = "0.1.0"
edition = "2024"
description = "J++ syntax (L1): lexer, parser, source AST, loader, and lowering to IR"
license = "MIT"

[dependencies]
jpp-ir = { path = "../jpp-ir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
