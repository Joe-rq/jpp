[package]
name = "jpp-ir"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "J++ IR (L0): keys and ids shared across crates, IR nodes, site and annotation tables"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
