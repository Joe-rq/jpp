# G4 + G5 四十八条猜点分类

整理：ir-impl-4（代理），2026-09-21。依据：`设计/G4-零上下文读者-构建器.md`、`设计/G5-零上下文读者-构建器二.md`。
三类：**文档缺**（语义已定，README 没写；已补，给出 §）；**语义未定**（规范 v0.1 没定或实现只能选一种；给提议与依据，供 Nature 定，实现先按提议走并登记 README §7）；**夹具级**（FakeClient / 假生成器 / 人答接口，与语言无关）。

## 计数

| 类 | G4 | G5 | G6 | 合计 |
|---|---|---|---|---|
| 文档缺 | 14 | 12 | 7 | 33 |
| 语义未定 | 6 | 9 | 6 | 21 |
| 夹具级 | 4 | 3 | 1 | 8 |
| 读者漏看（表里 / 正文有） | — | — | 7 | 7 |

G6 的 21 条里 7 条是读者自报「表里 / 正文有，我漏看或没把握」，单列一类不算文档缺。

## G4（24）

| 猜 | 一句话 | 类 | 落点 / 提议 |
|---|---|---|---|
| 1 | on / ctx 分工 | 文档缺 | README §2.1 |
| 2 | 向量化 cut 顺序对齐 | 文档缺 | §2.3、§9 `cut` 行「向量对齐」 |
| 3 | ask 重放后返回类型 | 文档缺 | §2.8：出口，taint=trusted |
| 4 | Mat 能否 == / 进 set | 语义未定 | **提议**：按内容哈希相等（同内容 = 同材料，与账本键一致，P3）；`bool/len/in` 是类型错。已实现，§9 `Mat` 行 |
| 5、6 | 人答 kind / 签名 / root | 夹具级 | §2.8 |
| 7 | 三档用 measure 还是 select | 文档缺 | §2.2：有序档位 → measure；无序标签集 → select |
| 8 | scale 是标签还是区间 | 文档缺 | §2.2 |
| 9、10 | FakeClient score 返回体 | 夹具级 | §4 |
| 11 | At(k) 的 k 是下标 | 文档缺 | §1 出口映射表 |
| 12 | hi/lo 对 measure 的意义 | 语义未定 | **提议**：measure 只用 hi（该档概率 ≥ hi+δ → At），lo 无意义；v0.1 §2.3 只写 test 的三值线。已实现 |
| 13 | loop 变式里读 .content 拆层 | 文档缺 | §2.9 |
| 14 | transform 返回元素是 Mat | 文档缺 | §2.7 |
| 15 | guard 传什么 | 文档缺 | §2.6 |
| 16 | 页面树 taint 自封 trusted | 语义未定 | **提议**：可信只能来自 S 库登记（`register_action(reason=)`），程序模块内自封报 W-self-trusted；登记表需 Nature 认可的动作清单（§2.11 修订）。已实现 warn |
| 17 | handle(keep=) 与 band 重跑 | 文档缺 | §2.5 |
| 18 | 普通 for 里 iter_seq | 文档缺 | §2.6 |
| 19 | Budget 字段 | 文档缺 | §2.10；§9 `Budget` 行 |
| 20 | escalate 抛还是返 | 语义未定 | **提议**：返回 `Escalated` 值、计预算，不抛（失败是值，J-12 同一精神）。已实现 |
| 21 | MatFuture 直接 return | 语义未定 | **提议**：返回值里的期物解析成 `Mat`（保来源链）。已实现 |
| 22 | match 守卫失败后的消费 | 语义未定 | **提议**：消费幂等；命中一次即消费。已实现 |
| 23 | Ignore 要不要消费 | 文档缺 | §2.5：只有 Unsure |
| 24 | rule 的 text 格式 | 夹具级 | §4 |

## G5（24）

| 猜 | 一句话 | 类 | 落点 / 提议 |
|---|---|---|---|
| 1 | gen 的 n 是上限 | 文档缺 | §2.7；§9 `gen` 行「n 上限」 |
| 2 | jv.lit 的 taint | 文档缺 | §1 材料行、§9 `lit` 行：trusted（§2.11 字面量 = trusted） |
| 3 | `case jv.Unsure():` 不绑 cause 算消费 | 语义未定 | **提议**：match 命中即消费，绑不绑 cause 无关（J-05 说的是「被 match 消费」）；**裸 `isinstance(e, jv.Unsure)` 不消费**（否则任何包装层一判种类 J-05 失效，Codex 评审踩实）。已实现，§2.5 |
| 4 | 向量化 judge 空输入 | 文档缺 | §9 `judge` 行「空输入」：`[]` |
| 5 | 攒起来问人怎么写 | 语义未定 | **提议**：`jv.ask` 一次一题（Pending 是程序级出口，B2）；批量交人 = `jv.escalate(列表)` 返回 `Escalated`，由外层处理；若要每条都进校准集，用 `jv.consume(exits, unsure=jv.escalate)`（逐条 ask，第一条 Pending 就挂起，恢复即重放）。已实现，§2.8 |
| 6 | transform 对 list[str] 逐个包 Mat | 文档缺 | §2.7；§9 `transform` 行「列表」 |
| 7 | 模板放 ref 还是 ctx；ref 是否渲染 | 文档缺 | §2.1：ref 是参照，渲染进状态（text 键 `ref`） |
| 8 | 条款编号从哪来 | 文档缺 | §2.3：出口与材料不带位置，用宿主 `enumerate` |
| 9 | measure 的 band 归档；δ 多大 | 语义未定 | **提议**：`Unsure("band")` 带 `detail["nearest_level"]`（最高概率档），归档由 handler 定；δ 取档案 `delta.score`。已实现 |
| 10 | for 里 gen 的 retry_seq | 文档缺 | §2.7（库已主动报 W-seq-const） |
| 11、12 | `.order()` 返回形态 / 分组 / 刷新 / 消费 | 文档缺 | §2.3 新段；§9 `Readings.order` 行：下标分组列表，组间从高到低，组内并列（差 ≤ δ），是刷新点，不消费，失败读数最后一档 |
| 13 | transform 收材料列表 | 文档缺 | §2.7；§9「实参」 |
| 14 | 普通 for 里 retry_seq | 文档缺 | §2.7 |
| 15 | transform 过滤为空返回 | 文档缺 | §9「空输入」：`[]` |
| 16 | select 的 over=[] | 语义未定 | **提议**：`judge` 时报错（select 需要候选集；I1 模型只在给定集合上分配概率，空集无题）。已实现 |
| 17 | Pick(k) 的属性名 | 文档缺 | §1 出口映射表：`.k` / `.level` / `.cause` |
| 18 | guard 能否收 Pick | 语义未定 | **提议**：不能；守卫是「一个被判为真的命题」（J-08 的合取项），Pick/At 是选择或档位；报文现在明说并指路再问一道 test 题。已实现 |
| 19 | Runtime(generator=) 与签名 | 夹具级 | §2.7：`fn(prompt, ctx: list[Mat], n, retry_seq) -> iterable` |
| 20 | Budget.escalate 默认 | 文档缺 | §2.10：全部字段默认 None = 不限 |
| 21 | Budget.layers 数什么 | 语义未定 | **提议**：只数 judge 层；只刷新 do 的那次不算层（层 = 一次融合调用的边界，P5）。已实现，§9「不计层」 |
| 22 | 假客户端 text 含 ref/ctx | 夹具级 | §4 |
| 23 | 模型调用失败整程序崩 | 语义未定（J-12 违反） | **提议 = 规范原意**：客户端 / 生成器 / do / transform 异常一律是值（`Unsure(fail)` / `[]` / Fail 材料），不崩。已修，`tests/test_jv_j12.py` |
| 24 | Escalated 进返回值 | 文档缺 | §9 `escalate` 行「返回值」：透传，不经 J-05 |

## 「语义未定」十五条一览（供 Nature 定）

1. Mat 相等性按内容哈希（G4-4）。
2. measure 只用 hi 线，lo 无意义（G4-12）。
3. 可信只能来自 S 库登记；自封报警；登记表需审核方（G4-16）。
4. escalate 返回值不抛（G4-20）。
5. 返回值里的期物解析成 Mat（G4-21）。
6. 消费幂等（G4-22）。
7. match 消费、裸 isinstance 不消费 Unsure（G5-3）。
8. 批量问人 = escalate(列表) 或 consume(unsure=escalate)（G5-5）。
9. measure band 带 nearest_level（G5-9）。
10. select over=[] 是错（G5-16）。
11. guard 不收 Pick/At（G5-18）。
12. Budget.layers 只数 judge 层（G5-21）。
13. 失败一律是值，不崩（G5-23，J-12 原意）。
14. `jv.mat` 在程序帧内对非标量报 W-literal-from-host（Codex 评审提出，J-11 补丁）。
15. `.order()` 是刷新点、不消费、失败读数末档（G5-11/12 的实现选择）。

## 判断

两轮各 24、集合不重叠，但**语义未定只占 15/48**；其余 33 条是文档与夹具。README §9 契约表把每个公开名字的七列写死并逐格测试，是对「文档缺」这一类的系统修法；「语义未定」十五条要 Nature 逐条定，定了就从 §7 偏差表升为规范条文。第六轮读者若仍 ≥ 20，问题就不在文档。

## G6（21，契约表之后；处置：ir-impl-5）

| 猜 | 一句话 | 类 | 落点 / 提议 |
|---|---|---|---|
| 1 | test 题的状态能否带 `over=` | 文档缺 | README §2.1 已有「over 只 select 用」但没说 test 忽略它；实测 test 与 select 共用带 over 的状态融合成一次调用（12 题 4 调用）。**已补**：§2.3 多题各自排序段顺带写明 |
| 2 | 守卫只认 Act，「可疑吗」得反着问 | 语义未定 | **提议**：守卫题必须是「放行为真」的肯定命题；否定题的 `Ignore` 不放行（`Ignore` 只是未越 lo 线，不是被校准的肯定命题；J-08 合取项 = test 题的 Act）。已实现（原本就拒 Ignore），§2.6 写明，§7-43 |
| 3 | 材料 untrusted 时守卫永远不过，完整路径没给 | 文档缺 | **已补** §2.6：两条路——`jv.ask` 答案；或经 `register_action(taint_out="trusted", reason=)` 的可信检测器产出 trusted 材料再判 |
| 4 | `register_action(inherit)` 的 `reason` 能否空 | 语义未定（读者标「有一半」） | **提议**：`reason` 是可信来源的证明，只对 trusted 必填；inherit / untrusted 可空。已实现，§9 `register_action` 行「空输入」 |
| 5 | 同批读数拆两列表 `cut` 两次算几层 | 读者漏看 | §2.10「一次 cut 通常一层」+ §3.1「已就绪的读数再 cut 不加层」；实测 1 层 |
| 6 | `match (a, f)` 元组模式 + `case _` 不消费 Unsure；用 `consume(drop)` 补但语义是交人 | 语义未定 → **静默项** | **提议**：通配不消费（J-05 只认命中 `case jv.Unsure()`），静态报 `W-wildcard-unsure`；丢弃与交人是两条记账：`jv.escalate(载荷, exits=[出口])` 出口随之消费并记 `consumed_by=escalate`；同帧既 drop 又 escalate 报 `W-drop-vs-escalate`。已实现，§2.5、§7-41/42 |
| 7 | 期物嵌在返回 dict 的 list 里会不会解析 | 文档缺 | **已补** §9 `do` 行：返回值递归解析（list / tuple / dict） |
| 8 | 两次独立 `judge(states, q)` 同段是否融合 | 读者漏看 | §2.3 有；**已补**一句明说「两次调用仍融合成一次调用」 |
| 9 | 多题向量能否直接 `.order()`、按哪题 | 文档缺 | **已补** §2.3：多题各自排序 = 分别 judge 再各自 order（`.order(q_index)` 也可） |
| 10 | δ 多大、组内谁先 | 文档缺 | **已补** §2.3：δ 取校准键记录的 `delta`，无记录取档案；组内顺序由宿主定 |
| 11 | 只 `.order()` 不 `cut` 会不会被查 | 读者漏看 | §9 `order` 行「消费：否」 |
| 12 | 嵌套循环里 `gen` 的 `retry_seq` 用哪个变量 | 文档缺 | **已补**（§2.7 已有「普通 for 用循环变量」；键含 ctx 哈希，外层不同 hunk 靠 ctx 区分够） |
| 13 | 不进槽的宿主检查要不要经 `transform` | 文档缺 | **已补** §2.7：只读 `.content`、不进槽的计算不必经 transform |
| 14 | `transform` 收 `list[Mat]` 能否原样返 Mat 保来源链 | 语义未定 → **静默项** | **提议**：输出元素与某输入材料内容相同 → 就是那份材料（来源链、taint、derived_from 保留），其余新建。已实现，§2.7、§7-39 |
| 15 | `transform` 列表判空：失败路径返回单个 fail 材料，`bool(Mat)` 抛 | 语义未定 | **提议**：失败值形状与成功时相同——列表型（`-> list` 注解或该函数曾返回列表）返回空 `jv.FailList`（`.fail` 记原因），否则单个 fail 材料；`on_fail` 两种都认。已实现，§2.7、§7-38 |
| 16 | select 的 `over` 只剩 1 个候选合不合法 | 语义未定 | **提议**：合法且平凡——不发调用，`cut` 得 `Pick(0)`（p=1，`detail["trivial"]`），冷校准亦然（H2：概率和恒为 1，单元素分布无读数可校准）。已实现，§7-40 |
| 17 | 选题 Unsure 直接 `case` 不 handle 算消费 | 读者漏看 | §2.5 有 |
| 18 | `Escalated` 载荷属性名 | 读者漏看（有一半） | §2.8 / §9 有 `Escalated(payload, note)`；现在还有 `.exits` |
| 19 | `Budget.layers` 该填几 | 读者漏看 | §2.10 有 |
| 20 | 假客户端 `on` 是 dict 时的形状 | 夹具级 | §4：`json.loads` 后 dict 原样 |
| 21 | 动作函数收到 Mat 还是原值 | 读者漏看（有一半） | §9 `do` 行「输入：材料」；六条示例用 `.content` |

**静默项两条怎么堵**：猜 6（本意交人却写 drop）——运行期 `W-drop-vs-escalate` + 静态 `W-wildcard-unsure` + `escalate(exits=)` 正路；猜 14（来源链静默丢失）——transform 输出按内容哈希匹配回原材料，不再新建。两条各有回归测试（`tests/test_jv_g6.py`）。

**语义未定六条**（供 Nature 定）：2 守卫只收肯定命题的 Act；4 `reason` 只对 trusted 必填；6 通配不消费、交人是独立记账；14 transform 子集 / 重排保留原材料；15 transform 失败值形状与成功时相同；16 单候选 select 平凡出口。

**判断**：第六轮 21 条里读者漏看 7、夹具 1，真正的文档缺 7、语义未定 6。契约表把「文档缺」从 12 压到 7，把「读者漏看」变成可数的一类；语义未定不随文档收敛，只随 Nature 裁决收敛。
