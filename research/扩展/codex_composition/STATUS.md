# 组合标准库施工状态

总目标：问题与方法可编程，组合后仍能继续组合；交付可运行库、两种算法和独立新增方法。

当前包：组合标准库与共同语言表示的最小贯通。上一包保留为“组合标准库初版”。

当前步骤：3 / 3，实施与检查完成，等待原设计节点验收。交付入口：[IR贯通验收.md](IR贯通验收.md)。

工作分工：Codex 修改本扩展目录；Claude 修改共同 `foundation/jv/plan.py` 并新增 `foundation/tests/test_jv_structure.py`。双方完成接口对齐与一轮定向修正。公开入口仍为 `from foundation import jv`。

实现选择：Python 3.12，接现有内核；本轮不另建 Rust 或 OCaml 执行器。

当前入口：`run.py integrate` / `test-integration` 使用工作内核；`run.py demo` / `test` 保留初版快照依赖。

初版成果保留：32 项检查通过；三种题型和真实动作的展开 / 封装 / 再嵌套两层行为一致；128 候选在固定观察下为 7 次 / 94 次；表达式三轮通过全部 9 个指定输入；独立作者写出预算分配并接入原生观察。历史输出见 `out/test-results.txt` 与 `out/results.json`。

本轮最终检查：库检查 40 项、当前工作内核贯通检查 11 项、共同计划器结构检查 16 项，共 67 项通过。完整对照源码另作为独立入口执行通过。肯定、否定、未决三种输入的两种写法等价；替换接受分支后，计划和实际动作同时由 1 变成 2，旧程序保持 1。

交接入口：`RESULTS.md`、`README.md`、`给Claude的交接.md`、`out/demo.html`。演示数据与滑块脚本已检查；浏览器工具限制本地文件 URL，未做视觉目视验收。

本轮范围：refine 只更新未决、factory 能力显式声明或保守未知、共同计划器识别结构、受控替换后描述与执行一致、直接内核用法对照。按最新指示暂停程序修复应用扩展，不另建运行时。

初版快照指纹：`be68c783b4b2475174a5efc366986a4b05c230bbba39a0927a85f8208b9a2bcf`（完整清单在 `kernel_manifest.json`）。只读快照含 36 个文件，保留不变。当前贯通所用 `plan.py`、`runtime.py`、`ir.py` 的文件指纹另存于 `out/ir-integration.json`；本轮只由 Claude 修改计划器，没有改 Runtime 或 ir.py。

实际决定：使用固定观察验证组合，没有真实 JEV 模型调用。真实客户端继续通过同一公开 Runtime 接入。

当前工作内核也已接通：三条完整程序、消融和重放实际运行通过；模块来源与指纹见 `out/working_kernel_check.json`。兼容了 Mat 禁隐式真假判断与新增的公开 Action 登记，无需修改内核。

消融结果：完整程序 3 次实际候选检查；去语义选择/度量仍 3 次；去反例过滤为 13 次。本例收益来自反例过滤，不能当作真实 JEV 增益。

当前边界：`bind`、`product`、`iterate` 在共同计划器中仍为明确的不透明节点；没有另建运行时，没有展开完整程序修复应用。下一步由原设计节点结合验收安排。

协作费用：与 Claude Code 对齐及修改使用了 Claude CLI，两次返回的费用合计约 1.752 美元；不能把整个协作过程称为零费用。JEV 读数均为合成，不计为真实模型实验。
